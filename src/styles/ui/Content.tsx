import { Col, styled } from "@nextui-org/react";

export const Content = styled("div", {
  margin: "$10 $15 $0 $15",
  minHeight: "auto ",
});

export const TextCol = styled(Col, {
  display: "flex",
  alignItems: "center",
  justifyContent: "flex-start",
});

export const Main = styled("div", {});
